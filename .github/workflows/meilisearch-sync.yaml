name: Build Deploy

# 在main分支发生push事件时触发。
on:
  push:
    branches:
      - main
      - master

env: # 设置环境变量
  TZ: Asia/Shanghai # 时区（设置时区可使页面中的`最近更新时间`使用时区时间）

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: build project ⚗️
        uses: actions/setup-node@v4
        with:
          node-version: "lts/Jod"
      - name: Run docs-scraper 🚀
        env:
          API_KEY: ${{ secrets.MEILISEARCH_API_KEY }}
          CONFIG_FILE_PATH: ${{ github.workspace }}/docs/public/data/config.json
          MEILISEARCH_HOST_URL: ${{ secrets.MEILISEARCH_HOST_URL }}
        run: |
          echo " 🏃‍♂️ Pulling docker images 🏃‍♂️  "
          docker pull getmeili/docs-scraper:v0.12.11
          echo " 🏃‍♀️ Running docs-scraper 🏃‍♀️  "
          docker run -t --rm \
            -e MEILISEARCH_HOST_URL=$MEILISEARCH_HOST_URL \
            -e MEILISEARCH_API_KEY=$API_KEY \
            -v $CONFIG_FILE_PATH:/docs-scraper/config.json \
            getmeili/docs-scraper:v0.12.11 pipenv run ./docs_scraper config.json
